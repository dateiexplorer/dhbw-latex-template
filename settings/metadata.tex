%!TEX root = ../document.tex

%------------------------------------------------------------------------------%
% Settings marked with * are required.
%------------------------------------------------------------------------------%

% Document language*
% If the language is not supported yet, you can define your own language file or
% use the default (en).
\newcommand{\setDocumentLanguage}{de}

% Document type*
% T3/1000, T3/2000, T3/3100, T3/3300
\newcommand{\setDocumentType}{Vorlage}

% Title*
% The title should not be longer than two lines.
\newcommand{\setDocumentTitle}{Dokumentation der Vorlage für wissenschaftliche
Arbeiten an der Dualen Hochschule Baden-Württemberg mit Beispielen}

% Author*
\newcommand{\setDocumentAuthor}{dateiexplorer}

% Matriculation number*
% Usually a seven-digit number, e.g. 5703134
\newcommand{\setMatriculationNumber}{???????}

% Course name*
% Find in on your student ID, e.g. MOS-TINF19B
\newcommand{\setCourse}{???-???????}

% Location of the university*
\newcommand{\setLocationUniversity}{????????????????????}

% Release date*
\newcommand{\setReleaseDate}{\today}

% Release location*
\newcommand{\setReleaseLocation}{????????????????????}

% Period*
% The duration the thesis was written through.
\newcommand{\setDocumentPeriod}{10.05.2021 - \today}

% Department*
% e.g. Angewandte Informatik
\newcommand{\setDepartment}{????????????????????}

% Lecture
% Optional, if this thesis was written for a specific lecture.
\newcommand{\setLecture}{}

% Degree (required if \setDocumentType{T3/3300})
\newcommand{\setDegree}{????????????????????}

% Name of the tutor*
\newcommand{\setTutor}{????????????????????}

% Name of the evaluator (required if \setDocumentType{T3/3300})
\newcommand{\setEvaluator}{}

% Company name
% Leave empty if not required. Only in combination with \setCompanyLocation.
\newcommand{\setCompanyName}{????????????????????}

% Company location
% Leave empty if not required. Only in combination with \setCompanyName.
\newcommand{\setCompanyLocation}{????????????????????}

% Restriction
% Is this document restricted to the public? Set `yes` if so.
\newcommand{\restrictDocument}{no}

% Electronic
% Has this document an electronic version? Set `yes` if so.
\newcommand{\hasElectronicVersion}{yes}

%------------------------------------------------------------------------------%
% Company logo
% Show the company logo on the titlepage? Set `yes` if so.
%
% If the company logo is not rendered correctly, you can change the size inside
% of the cover module in `modules/cover.tex`
\newcommand{\showCompanyLogo}{yes}